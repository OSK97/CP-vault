{
  "id": "algorithm-any-of",
  "title": "any_of",
  "category": "STL Algorithms (Counting)",
  "aliases": [
    "any_of",
    "any matching"
  ],
  "inputs": [
    {
      "id": "container",
      "label": "Container Variable",
      "type": "string",
      "default": "v",
      "validation": {
        "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
        "message": "Must be a valid C++ identifier"
      }
    },
    {
      "id": "predicate",
      "label": "Predicate Function / Lambda",
      "type": "string",
      "default": "[](int x) { return x > 0; }"
    }
  ],
  "template": "bool any = any_of({{container}}.begin(), {{container}}.end(), {{predicate}});"
}