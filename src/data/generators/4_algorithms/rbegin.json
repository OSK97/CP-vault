{
  "id": "algorithm-rbegin",
  "title": "rbegin",
  "category": "STL Algorithms (Ranges)",
  "aliases": [
    "rbegin",
    "std::rbegin",
    "iterator boundary"
  ],
  "inputs": [
    {
      "id": "container",
      "label": "Container Variable",
      "type": "string",
      "default": "v",
      "validation": {
        "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
        "message": "Must be a valid C++ identifier"
      }
    }
  ],
  "template": "rbegin({{container}})"
}